using UnityEngine;

public class Enemy_Battle_director_State : EnemyState
{
    // Start is called once before the first execution of Update after the MonoBehaviour is created
    public Enemy_Battle_director_State(Enemy enemy, Enemy_StateMachine enemyStateMachine, string animBoolName) : base(enemy, enemyStateMachine, animBoolName)
    {
    }

    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
